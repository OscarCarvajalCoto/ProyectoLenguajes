@model ProyectoLenguajes.Models.Rating

@{
    ViewData["Title"] = "Rating Details";
}

<h1>Rating Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            Rating
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.rating1)
        </dd>
        <dt class = "col-sm-2">
            UserName
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.app_user)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-rating_id="@Model.rating_id">Edit</a> |
    <a asp-action="Index" asp-route-ms_id="@Model.ms_id">Back to List</a>
</div>
